import './style.scss';

import React, { memo } from 'react';

const BottomBar = (): JSX.Element => {
    return <section className="bottom-bar-container"></section>;
};

export default memo(BottomBar);
